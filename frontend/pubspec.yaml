name: doc_query
description: "an AI-powered document platform to extract, analyze, and retrieve information from documents efficiently."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  desktop_drop: ^0.7.1
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.7+1
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
